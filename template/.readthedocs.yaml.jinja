build:
  jobs:
    post_build:
      - make reports
    post_install:
      - make dev-docs
      - python -m pip install --upgrade git-changelog
    pre_build:
      - make changelog
  os: ubuntu-22.04
  tools:
    python: '{{ default_py }}'
sphinx:
  fail_on_warning: true
version: 2
